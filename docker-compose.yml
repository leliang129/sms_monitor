services:
  sms-monitor:
    image: ghcr.io/leliang129/sms_monitor:latest
    container_name: sms-monitor
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./config.json:/app/config.json
    environment:
      - TZ=Asia/Shanghai
